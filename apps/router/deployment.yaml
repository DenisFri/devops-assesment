apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-router
  labels:
    app: python-router
    prometheus: enabled
spec:
  replicas: 1
  selector:
    matchLabels:
      app: python-router
  template:
    metadata:
      labels:
        app: python-router
    spec:
      containers:
      - name: router
        image: denisfri/python-router:latest
        ports:
        - containerPort: 80
          name: http
